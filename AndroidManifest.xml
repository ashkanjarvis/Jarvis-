<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- گوش دادن به صدا برای تشخیص کلمه‌ی فعال‌ساز (Jarvis) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- نمایش دایره‌ی جارویس روی همه‌ی برنامه‌ها -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- سرویس فورگراند برای گوش دادن دائمی -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- لیست برنامه‌های نصب‌شده برای باز کردن اپ با دستور صوتی -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <!-- دوربین برای تشخیص دست (فاز بعدی / استاب) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Jarvis"
        android:theme="@style/Theme.Jarvis"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Jarvis">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".OverlayService"
            android:foregroundServiceType="microphone"
            android:exported="false" />

    </application>
</manifest>
